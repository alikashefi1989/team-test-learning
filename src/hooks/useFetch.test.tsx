import React from 'react';

describe('useFetch hook', () => {
    it('is delicious', () => {
        expect(1).toBeTruthy();
    });

    it('is not sour', () => {
        expect(false).toBeFalsy();
    });
});
